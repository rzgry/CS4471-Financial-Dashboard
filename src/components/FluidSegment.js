import React from 'react';
import { Segment } from 'semantic-ui-react';

const FluidSegment = ({ children }) => <Segment style={{ height: '100%' }}>{children}</Segment>;

export default FluidSegment;
